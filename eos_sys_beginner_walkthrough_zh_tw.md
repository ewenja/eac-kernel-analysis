# `EasyAntiCheat_EOS.sys` 白話導讀

更新日期: `2026-05-10`  
對應樣本: `C:\Users\<user>\Desktop\EAC\EasyAntiCheat_EOS.sys`

> 這篇是給新手、一般程式開發者，還有想快速理解樣本重點的人看的。  
> 不走太重的逆向術語，盡量講人話。  
> 如果你要看完整硬證據、位址、指令切片，請再回去看
> [`eos_sys_2026_05_static_revalidation.md`](eos_sys_2026_05_static_revalidation.md)。

---

## 先一句話講完

這份新版 `EasyAntiCheat_EOS.sys` 很明顯不是「普通 Windows driver」。

它的樣子比較像：

- 外表看起來很乾淨
- 直接匯入的 API 很少
- 真的邏輯被藏在很大的保護區裡
- 內部控制流不是一般工程師平常會寫出來的樣子

如果要再白話一點：

**它不是把功能直接攤開給你看，而是先把自己包得很厚，再慢慢在裡面跑一大串 state machine。**

---

## 1. 這份樣本到底證明了什麼

這一輪我們已經能很穩地確認幾件事。

### 1. 它是正式簽章的 production driver

不是隨手拼的測試檔，也不是未簽章樣本。

目前已驗證到：

- 檔案版本 `3.2.0.0`
- 正式 Authenticode 簽章有效
- 是 `PE32+ / AMD64 / Native subsystem`

這代表它就是一個真的要在 Windows kernel 裡跑的東西，不是單純研究樣本。

### 2. 它的 import surface 很小

目前直接看到的 import 只有兩個：

- `FLTMGR.SYS!FltRegisterFilter`
- `ntoskrnl.exe!__chkstk`

這件事很重要，因為它代表：

- 你不能只靠 import table 理解它會做什麼
- 很多能力都被藏到內部 resolver / dispatcher / 包裝層裡

對新手來說，這是第一個要改掉的習慣：

**看到 import 很少，不代表功能很少。**

### 3. 它有非常大的保護執行區

目前最顯眼的是：

- `sect_7`

這個區塊非常大，而且不是普通程式碼區那種「函式一個接一個、很好看懂」的風格。

我們已經驗證到：

- `sect_7` 裡至少有大量 runtime function entry
- 不是只有一整塊垃圾碼
- 裡面有很多固定大小、重複模板化的小函式

所以比較白話的說法是：

**這裡像是一個巨大保護工廠，裡面有很多很小、很像積木的運算片段。**

### 4. 它不是單純在搬資料

這點是這輪靜態分析最值得寫給新手看的地方。

從 `0x1402E9770` 後面一路往下挖，我們已經確認裡面有很多不是單純 load/store 的大 stage。

這些大 stage 的共同特徵是：

- 一開始會先比很多常數
- 中間會做固定寬度欄位切割
- 會有大量 `seta / setb / sete / setne`
- 會用很多 `and / shr / shl / add / sub / xor / imul`
- 最後把結果寫回同一個 state block
- 再跳去下一個 stage

你可以把它先想成：

**很多小規則、很多小位元欄位、很多條件位，全部混在一起往前推的一條 pipeline。**

---

## 2. 目前最像什麼

現在如果不用太學術的說法，這份樣本最像：

### 一個被重度保護的內部狀態機

它不是「讀一個值，算一下，return」那種普通函式。

比較像這樣：

1. 讀 shared state
2. 拿某些欄位做比較
3. 把結果切成不同 bit-width 的小欄位
4. 再重組
5. 把新結果寫回 state
6. 根據算出來的下一個位址跳走

而且這個流程會一直重複。

### 一條很長的 compare-driven pipeline

這個詞比較重要。

因為目前我們看到的很多大段，都不是隨機亂算，而是明顯有：

- compare
- decision bit
- carry / borrow 類效果
- 再把這些 decision bit 混進後續欄位重組

所以它不是單純「數學很亂」，而是：

**它在用很多條件判斷，慢慢推一個被保護的內部狀態。**

---

## 3. 為什麼這種寫法難分析

如果你是一般程式員，會覺得這種東西特別煩，大概有三個原因。

### 1. 它不是正常控制流

平常我們看程式：

- 呼叫函式
- return
- 再呼叫下一個

但這裡很多地方是：

- 先算一個目標位址
- 再 `jmp` 過去

也就是說，你很難一眼看出真正的流程圖。

### 2. 它把資料切得很碎

目前已驗證到的欄位寬度非常多，例如：

- `12-bit`
- `13-bit`
- `26-bit`
- `28-bit`
- `34-bit`
- `36-bit`
- `39-bit`

這表示它不是拿一整個 `uint64_t` 直直算，而是把資料拆成很多小塊在重組。

對新手來說，這種東西最容易讓人看花掉。

### 3. 它同時有大 stage 跟小 stub

也就是：

- 有些函式很短，像 route-builder
- 有些函式很大，像真正處理 state 的主體

如果你不先分清楚哪一段只是「改 slot 然後跳走」，哪一段才是「真的在算東西」，會很容易迷路。

---

## 4. 目前已經挖到哪裡

目前不是只有看入口，也不是只看 section 名稱。

我們已經一路沿著主要 pipeline 往下補到：

- `0x1416A95AE`
- `0x1416AABEE`
- `0x1416ABBA0`

其中最近兩個已經補成完整 stage。

### `0x1416AABEE`

這段比較像：

- 單層 shared-state stage
- 有 `39-bit` limb 重組
- 混了 byte / `1-bit` decision packing
- 會回寫多個 state slot

白話講：

**它不是只做一次判斷，而是把很多小判斷打包後，再把結果灌回主狀態。**

### `0x1416ABBA0`

這段更適合給新手當例子，因為它的寬度結構很明顯。

目前可穩定描述成：

- 前段主打 `12-bit / 13-bit`
- 中段切到 `26-bit`
- 後段再進到 `34-bit`
- 同時混 byte 級 decision packing

白話講就是：

**同一段裡，它先把資料拆成比較小的塊處理，後面再慢慢拼成比較大的結果。**

---

## 5. 現在還不能亂講什麼

這點很重要，尤其是給新手。

我們這輪已經知道很多東西，但還是有幾件事不能講太滿。

### 1. 不能直接說「完整 VM ISA 已證明」

雖然 `sect_7` 很像保護執行區，也有很多模板化 micro-op / stub。

但目前比較穩的說法還是：

- 有大型保護區
- 有大量模板化小函式
- 有 dispatcher / route-builder / state stage

不能直接跳成：

- 「已經完整證明它就是某個完整 VM 指令集」

### 2. 不能只靠 `FltRegisterFilter` 就說它一定做了完整 minifilter 初始化

目前我們是有看到：

- `FltRegisterFilter` 存在

但還沒把整條註冊路徑乾淨抓出來。

所以現在比較準的是：

- 它具備這個能力
- 但完整呼叫路徑還在追

### 3. 不能把每段大數 / bit-slice 算法直接命名成特定演算法

現在看到很多：

- fixed-width limb
- carry-style 行為
- compare-driven recomposition

但還不能每看一段就直接寫成：

- 這裡一定是某某密碼演算法

比較保守、也比較不會被打臉的寫法是：

- compare-driven mixed-width recomposition
- state normalization
- decision-bit packing

---

## 6. 如果你是新手，應該怎麼讀這份樣本報告

最推薦這樣讀：

### 第一步

先看這篇，建立整體感。

重點不是背位址，而是先知道：

- 這份 driver 不是普通寫法
- 它有很多 state stage
- 它在大量做條件位和固定寬度欄位重組

### 第二步

再看：

- [`eos_sys_2026_05_static_revalidation.md`](eos_sys_2026_05_static_revalidation.md)

但讀法不要一行一行硬啃。

先抓：

- 入口怎麼進
- `sect_6` / `sect_7` 各是什麼角色
- 哪些是 short route-builder
- 哪些是 large stage

### 第三步

把每個 stage 先當成這種格式記：

1. 它先讀哪些欄位
2. 它主要比哪些常數
3. 它切了哪些 bit-width
4. 它最後寫回哪些欄位
5. 它尾端跳到哪裡

只要你先養成這個閱讀習慣，會比一開始就想「完全還原演算法」輕鬆很多。

---

## 7. 如果你是程式員，最值得帶走的觀察

如果你平常不是做逆向，而是做一般軟體、遊戲、kernel、平台工程，我覺得這份樣本有幾個很值得學的點。

### 1. 保護不一定靠一個大招

這份樣本最有感的地方不是某個單點技巧，而是：

- 極小 import surface
- 大保護區
- 間接跳轉
- 模板化 stub
- 條件位重組

全部疊起來，分析成本就上去了。

### 2. 「資料格式設計」本身也可以是防禦

當你把狀態拆成：

- 12-bit
- 13-bit
- 26-bit
- 34-bit

再讓它跨很多 stage 重組，外人要一次看懂就會很累。

這不一定代表它在做什麼神祕黑魔法，但很明顯有在提高靜態理解成本。

### 3. 保守寫結論，比帥氣命名更重要

這輪報告一直刻意用：

- route-builder
- state shaper
- mixed-width recomposition stage

而不是急著替每段亂取大名。

原因很簡單：

**安全研究最怕的不是還沒挖完，而是太早自信。**

---

## 8. 給 Epic / EAC 團隊的人一句話總結

如果這篇剛好有被 EAC 或 Epic 內部的人看到，我會用很平實的方式總結這份樣本：

這個 build 最讓人有感的，不是「用了某個新奇演算法」，而是它把控制流、state layout、bit-width recomposition 和 pointer-mediated side write 綁得很緊。

換句話說：

**它的防護價值，不只是藏 API，而是讓分析者即使進到主體，也很難快速建立乾淨語意。**

---

## 最後一句

如果你現在看完還覺得：

「我還是沒有辦法直接看懂組語」

那很正常。

這篇的目的不是讓你一夜之間會逆向，而是讓你先知道：

- 這個樣本在幹嘛
- 哪些地方已經被證明
- 哪些地方還在保守追
- 你接下來該怎麼讀，不會一開始就被淹死

這樣就夠了。

---

## 導航

- 上一篇：[新版樣本靜態再驗證](eos_sys_2026_05_static_revalidation.md)
- 相關閱讀：[EAC 總報告（新手也看得懂版）](eac_beginner_report_zh_tw.md)
- 回索引：[README.md](README.md)
